import os
from setuptools import setup, find_packages

setup(
    name = 'simpleapi',
    version = '0.0.1',
    description = ('simple rest api'),
    keywords = 'simple rest api',
    include_package_data = True,
    packages = find_packages(),
    install_requires = [

    ],
    entry_points = [
        
    ]
)